apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillinggeothermalpropertieswebappclient.fullname" . }}
  labels:
    {{- include "norcedrillinggeothermalpropertieswebappclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillinggeothermalpropertieswebappclient.selectorLabels" . | nindent 4 }}
